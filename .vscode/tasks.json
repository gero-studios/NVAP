{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "nvap-setup",
            "type": "shell",
            "command": "powershell",
            "args": [
                "-NoProfile",
                "-Command",
                "if (-not (Test-Path '.venv')) { python -m venv .venv }; & ./.venv/Scripts/Activate.ps1; python -m pip install --upgrade pip; python -m pip install -e '.[dev]'"
            ],
            "isBackground": false,
            "problemMatcher": [],
            "presentation": {
                "reveal": "always",
                "panel": "new"
            }
        }
    ]
}
